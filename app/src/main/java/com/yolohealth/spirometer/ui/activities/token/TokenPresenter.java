package com.yolohealth.spirometer.ui.activities.token;

import com.yolohealth.spirometer.model.tokenresponse.TokenParams;

public interface TokenPresenter {
    void token(TokenParams tokenParams);
}
